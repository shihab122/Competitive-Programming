#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[100];
    //for(int i=0; ;)
    cin>>arr[100];
    int a=arr[sizeof(arr[])];
    cout<<sizeof(arr[]);
    int arr2[sizeof(arr)-1];
    int arr3[sizeof(arr)-1];
    for(int i=0;i<(sizeof(arr)/2)-1;i++)
        arr2[i]=arr[i];
//    for(int i=(sizeof(arr)/2))
    return 0;
}
