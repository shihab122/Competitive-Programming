#include<bits/stdc++.h>

using namespace std;

struct Parent{
    int u, v, s, e;
};

int main(){

    int t;
    scanf("%d", &t);
    while(t--){
        int n;
        scanf("%d", &n);
        Parent par[n];
        int l, r, a1 = 0, a2 = 0, arr[n];
        while(n--){
            scanf("%d %d", &l, &r);

        }
    }

    return 0;
}
