#include<bits/stdc++.h>

using namespace std;

int main(){
    cout<<setprecision(2);
    int n,k,a=0,sum=0;
    cin>>n>>k;
    for(int i=0;i<n;i++){
        int v;
        cin>>v;
        sum+=v;
    }
    while(1){
        float g,h;
        g=sum/n;
        if((g-(int)g)>=0.5)

    }
    return 0;
}
